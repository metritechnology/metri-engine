{
  "entity": "iot_harvester_config",
  "engine": "oltp",
  "is_system": false,
  "track_history": true,
  "attributes": [
    {
      "name": "id",
      "type": "uuid",
      "is_system": false
    },
    {
      "name": "asset_id",
      "type": "reference",
      "entityRef": "asset",
      "cardinality": "one",
      "required": true
    },
    {
      "name": "target_url",
      "type": "string",
      "required": true,
      "description": "El REST API Endpoint o Dirección IP del Activo/PLC."
    },
    {
      "name": "port",
      "type": "integer",
      "required": false,
      "description": "Puerto de red de conexión opcional (ej. 502 para MODBUS TCP, 80/443 para HTTP, 50051 para gRPC)."
    },
    {
      "name": "polling_interval_seconds",
      "type": "integer",
      "default": 300,
      "required": false
    },
    {
      "name": "schedule_type",
      "type": "enum",
      "options": [
        "INTERVAL",
        "CRON"
      ],
      "default": "INTERVAL",
      "required": true
    },
    {
      "name": "cron_expression",
      "type": "string",
      "required": false
    },
    {
      "name": "auth_type",
      "type": "enum",
      "options": ["BEARER_TOKEN", "BASIC_AUTH", "API_KEY", "NONE"],
      "required": true
    },
    {
      "name": "credentials",
      "type": "string",
      "sensitive": true,
      "required": true,
      "description": "Credenciales puras de la API (User/Password o Token). Protegidas automáticamente in-flight por el motor KMS (Envelope Encryption)."
    },
    {
      "name": "payload_format",
      "type": "enum",
      "options": ["JSON", "XML", "GRPC"],
      "default": "JSON",
      "required": true,
      "description": "El formato esperado de la respuesta de la API para que el Worker de Go ensamble el decodificador correcto."
    },
    {
      "name": "http_method",
      "type": "string",
      "default": "GET",
      "required": true,
      "description": "Método HTTP utilizado para interrogar la API (GET, POST, etc.)."
    },
    {
      "name": "http_headers",
      "type": "json",
      "description": "Cabeceras HTTP en formato JSON enviadas con la petición de interrogación."
    },
    {
      "name": "request_payload",
      "type": "json",
      "description": "Cuerpo/payload JSON enviado en las peticiones HTTP (generalmente para POST/PUT)."
    },
    {
      "name": "url_query_params",
      "type": "json",
      "description": "Parámetros de consulta (Query Params) de la URL en formato JSON."
    },
    {
      "name": "mapping_directive",
      "type": "json",
      "description": "Regla de transmutación para aplanar el JSON extraño del tercero."
    }
  ],
  "events": {
    "create": "IOT_HARVESTER_CONFIG_CREATED",
    "update": "IOT_HARVESTER_CONFIG_UPDATED",
    "delete": "IOT_HARVESTER_CONFIG_DELETED"
  }
}
